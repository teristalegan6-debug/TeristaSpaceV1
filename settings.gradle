pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // Add any custom repositories here if needed in future
        // maven { url "https://jitpack.io" }
    }
}

rootProject.name = "TeristaSpace"
include ':app'
include ':Bcore'
include ':black-reflection'
include ':compiler'
include ':native'
include ':sandbox'
include ':proxy'
include ':virtual-device'
include ':virtual-services'
include ':virtual-fs'
include ':virtual-process'
